# 첫 프로토타입 검증 기록

2026-09-19 / Windows x64 / Node 24.13.0.

## 실행한 검사

- `npm test`: 콘텐츠 검증, TypeScript 타입 검사, Nakama ES5 번들 생성 성공. 자동 테스트 **32개 통과**.
- `node tools/simulate.mjs`: 4인·8인, 시드 1·42·2026으로 총 6경기 완주.
- `git diff --check`: 추적 파일의 공백 오류 없음. Git의 LF→CRLF 안내는 출력됨.

HTTP 테스트는 실제 로컬 소켓에 네 세션을 연결해 공유 상태, 소유권, 중복 요청,
토큰 없는 접근, 참가자 인수 거부와 재접속을 확인했다.
Nakama 테스트는 Node VM에서 번들을 로드해 콜백을 실행했다. 동시 입장 4자리 예약과
만료된 입장 요청의 정원 초과·세션 덮어쓰기 회귀 테스트를 포함한다.

## 자동 경기 결과의 해석

스크립트 전략으로 36개 개인 라인이 모두 클리어했고 18개 스토리가 성공했다.
게임 내 시간은 약 604~612초다. 현재 밸런스가 쉬운 기능 검증용임을 보여준다.
사용자가 원하는 어려운 정식 난도가 완성되었다는 뜻은 아니다.

별도의 합성 부하 실험은 참가자당 유닛 30기·적 70기를 놓고 100스텝을 측정했다.
측정 환경에서 코어 한 스텝 p95는 4인 0.045ms / 8인 0.069ms였다.
표시 상태 JSON 최대 크기는 각각 39,465 / 78,869바이트였다.
부하 유지를 위해 시계·과밀 카운터·공격 대기시간을 매 표본 초기화한 합성 실험이다.
수치는 실행마다 달라지며 Nakama 처리·통신량·Unity 렌더링·iPhone 성능을 포함하지 않는다.

## 아직 실행하지 못한 검사

- Unity 에디터에서 C# 컴파일, 실제 화면·터치·애니메이션 확인.
- 실제 Nakama 및 PostgreSQL 프로세스에서 통합 실행.
- Unity 네 클라이언트 및 네 사람의 협동 플레이.
- iPhone 빌드·서명·성능·백그라운드 복귀.
- 영구 기록/해금 저장과 지연·패킷 손실 실험.

Unity/Docker는 PATH와 기본 설치 경로에서 확인되지 않았으며 다른 위치 설치 여부는 미확인이다.
이 항목들은 자동 테스트 통과로 대체하지 않는다.
